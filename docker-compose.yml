services:
  dashdrop:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - PORT=8080
      - DATA_DIR=/data
      - MAX_UPLOAD_BYTES=5242880
      - MAX_THUMB_BYTES=1048576
      - PUBLIC_PATH_PREFIX=/drop
    restart: unless-stopped
